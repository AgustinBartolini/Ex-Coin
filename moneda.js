let moneda = Math.floor(Math.random() * 2);

    if (moneda == 0) document.write ("Cruz");

    if (moneda == 1) document.write ("Cara");

    function refresh(){
        window.location.reload("Refresh")
      }